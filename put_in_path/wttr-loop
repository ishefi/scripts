#!/bin/sh
# Weather in terminal with auto-refresh
# Usage: wttr-loop [interval_seconds]
#   interval_seconds: refresh interval (default: 600 = 10 minutes)

interval=${1:-600}

while true; do
    clear
    wttr
    sleep "$interval"
done
