Put all files from this folder in /usr/local/bin/

After you have done so, you can use following commands:

Open Chrome:

```
chrome [filename]
```

Delete git branches that were deleted from remote:
```
git debranch
```
